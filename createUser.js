/**
 * Created by User on 23/03/2017.
 */

module.exports = {

    init: function (res,userName,password,email,addr,telNo,location) {
        //check username duplication

        //hash password with a randomly generated salt

        //send email to user for email verification

        //save userName, hashed password, salt, addr, email, telNo, location to db

    }
};